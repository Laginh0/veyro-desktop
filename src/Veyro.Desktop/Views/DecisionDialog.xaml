<Window x:Class="Veyro.Desktop.Views.DecisionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="470" SizeToContent="Height" MaxHeight="680"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="Transparent" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="{DynamicResource SurfaceRaisedBrush}" BorderBrush="{DynamicResource BorderStrongBrush}"
            BorderThickness="1" CornerRadius="18" Padding="24" Margin="22" Effect="{DynamicResource ShadowHigh}">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="48" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <Border Width="42" Height="42" CornerRadius="13" Background="{DynamicResource PrimarySoftBrush}" VerticalAlignment="Top">
                    <TextBlock x:Name="IconText" Text="&#xE72E;" FontFamily="Segoe Fluent Icons" FontSize="18" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="12,1,12,0">
                    <TextBlock x:Name="EyebrowText" Style="{StaticResource Eyebrow}" />
                    <TextBlock x:Name="TitleText" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="19" FontWeight="SemiBold" Margin="0,5,0,0" TextWrapping="Wrap" />
                </StackPanel>
                <Button Grid.Column="2" Content="&#xE711;" FontFamily="Segoe Fluent Icons" Style="{StaticResource GhostButton}" Padding="7" MinWidth="34" ToolTip="Fechar" Click="Decline_Click" />
            </Grid>
            <Border Grid.Row="1" Background="{DynamicResource SurfaceMutedBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,20,0,0">
                <TextBlock x:Name="MessageText" Style="{StaticResource BodySecondary}" LineHeight="20" />
            </Border>
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock Text="Conexão direta protegida" Style="{StaticResource Caption}" VerticalAlignment="Center" />
                <Button x:Name="DeclineButton" Grid.Column="1" Style="{StaticResource SecondaryButton}" IsCancel="True" Margin="0,0,9,0" Click="Decline_Click" />
                <Button x:Name="AcceptButton" Grid.Column="2" IsDefault="True" Click="Accept_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
